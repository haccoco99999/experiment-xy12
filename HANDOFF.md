# HANDOFF.md: notes between assistants

Read `AGENTS.md` first (the rules), then this file. Only one assistant works in the folder at a time.

## How to hand over

Before you stop, add an entry at the top of the log below (newest first). Keep it short and factual:

- What you did (files) and what you tested. Say what you did not test.
- The test result of `node --test "tests/*.test.js"`.
- What is left and what the next assistant should do first.
- Open questions for the user.

Keep the last 5 entries. Delete older ones; git history keeps them.

## Where things stand

- Done: Experiment 1 (mung bean), 2 (cabbage), 3 (chicks), 4 (lab rat), 5 (tomato flower) and 6 (new plants from four parts). Experiments 2, 4, 5 and 6 were committed together on 2026-09-20 after the user said yes (commit `d1f2cbc`). When the user said "push all" on 2026-09-20, everything on `main` was pushed to `origin` (private repo `haccoco99999/experiment-xy12`).
- Not built (their cards say "Sắp có"): 7, 8. Order: 7 next, then 8 (the standing order below). Experiment 7 has its logic and tests only (`js/logic/exp7.js`, `tests/exp7.test.js`, committed as work in progress); no texts, models or wiring yet. Experiment 8: an earlier build sits on the local branch `exp8` (commit `701178a`: `js/logic/exp8.js`, `js/content/exp8.vi.js`, `js/experiments/exp8.js`, `index.html`, `HUONG-DAN.txt`; no tests, models or SPEC-NOTES rows). The user said its worktree `v5-exp8` was an error and had the folder deleted on 2026-09-20. The branch itself is kept, not merged and not pushed. Ask the user before deleting it, and build Experiment 8 from the specification as usual.
- Not decided: which assistant builds each next experiment. The user decides.

## Standing order from the user (2026-09-20)

Build the remaining experiments one after another until all 8 are done: 2, 4, 5, 6, 7, 8. Watch the usage limits. At 90% (the 5-hour session or the weekly limit), finish the step in progress, note the state in the log below, pause until that limit resets, then continue without asking. Do not commit or push unless the user asks.

## Suggested split of work

This is a suggestion from the plan. None of these tools has been tried on this project yet.

- Codex: `js/logic`, `js/content` and `tests`. Node can check this work, and its documentation shows no built-in browser.
- Antigravity: 3D models, `js/experiments`, CSS, and looking at the page in a browser with screenshots. Its documentation says Chrome must be the default browser, and this site has only been tried on Edge.
- Claude: review, integration, `HUONG-DAN.txt`, `SPEC-NOTES.md` and commits.

Experiments edit shared files, so hand over only at a clean point: the tests pass and this file is updated.

## First-time check for each new assistant

Open the folder `D:\Code\Thí nghiệm\v5` in the assistant's app and paste this:

> Read AGENTS.md and HANDOFF.md. List the five rules, one line each. Then run `node --test "tests/*.test.js"` and tell me how many tests passed. Do not change any file.

A good answer lists all five rules (including the 90% usage rule and Vietnamese commit messages) and reports the same number of passing tests as the latest log entry. The folder name has a space and Vietnamese letters, so this also shows that the path works in that app.

## Log (newest first)

### 2026-09-20 (latest), Claude: Experiment 7 PAUSED after the logic and tests (weekly usage reached 90%)

- Usage when I stopped: 5-hour 19% (resets 2026-09-20T06:30Z = 13:30 local), weekly 90% (resets 2026-09-21T15:00Z = 22:00 local). The weekly limit is the binding one. Per rule 5 I started no new work. A one-shot resume is scheduled for 22:03 local on 21 September (CronCreate; it only fires if the Claude window stays open and idle).
- Done: read the whole Experiment 7 spec (lines 10942-13163; notes below). Wrote `js/logic/exp7.js` (pure rules: the two butterflies, mating, fertilization, `canContinue/applyContinue`, magnifier decisions for eggs / larva / pupa, hatching on days 3 and 7, feeding, three moults, ruler length, pupation, 3D cut and four parts, metamorphosis, sorting of the four cards, `stepStates` for the eight bar steps, `promptKey`, `dayStates`) and `tests/exp7.test.js` (10 tests). `node --test "tests/*.test.js"`: 98 tests, all pass. The `decide*` functions return message keys (`maleOff`, `femaleOff`, `femaleLeaf`, `maleFar`, `lensNothing`, `lensEggsOff`, `lensPupaOff`, `leafFar`, `rulerFar`, `partOff`, `sortWrong`) and `promptKey` returns task keys (`placeTask`, `mateTask`, `layTask`, `lensEggsTask`, `feedTask`, `lensPupaTask`, `partTask`, `sortTask`). `js/content/exp7.vi.js` must expose these keys in `MD.msg` or `APP`, with the sentences copied from the spec (lines are noted below). Larva lengths in `LENGTH_MM` (3, 8, 16, 28, 42 mm) are our own numbers: the spec gives none. Log that gap in `SPEC-NOTES.md`.
- Commits and push: the user said yes, so Experiments 2, 4, 5 and 6 are committed in one `feat` commit (`d1f2cbc`). Then the user said "push all" and asked to publish `docs/` too (the spec copy, 299 KB; the repo is private: an anonymous request returns 404). So `docs/` was committed in its own `docs` commit, AGENTS.md and GEMINI.md no longer say "never add docs/", and the two Experiment 7 files were committed as work in progress (`feat(exp7)`). Everything on `main` is pushed. The user also had the stray worktree `v5-exp8` deleted (see "Where things stand").
- Still not written for Experiment 7: `js/content/exp7.vi.js`, models (butterflies, leaf with bites, larva, ruler, eggs, pupa with cut, emerging adult), `js/experiments/exp7*.js`, script tags in `index.html`, CSS, shots scenarios, `HUONG-DAN.txt`, `SPEC-NOTES.md`.
- SPEC NOTES (line numbers are in `docs/THÍ NGHIỆM 1909.md`):
  - Tray (11031-11037): Bướm đực, Bướm cái, Kính lúp, Lá cây (tươi), Thước đo. Progress bar (11003-11012): BƯỚM ĐỰC + BƯỚM CÁI → GIAO PHỐI → THỤ TINH → BƯỚM CÁI ĐẺ TRỨNG → TRỨNG → SÂU NON → NHỘNG → BƯỚM TRƯỞNG THÀNH (8 steps, like the eight-step bar of Experiment 6; the CSS for eight steps exists).
  - 6.1-6.3 (11306-11387): male dropped in the observation area = flies to the branch, label BƯỚM ĐỰC, message "Bướm đực là bướm trưởng thành có khả năng tham gia sinh sản."; wrong = "Hãy đặt bướm đực vào khu vực quan sát." (back to tray). Female: label BƯỚM CÁI; wrong "Hãy đặt bướm cái vào khu vực quan sát.". Both placed: they fly together on one branch, camera zooms a little, label "BƯỚM ĐỰC + BƯỚM CÁI", message "Bướm đực và bướm cái trưởng thành có thể sinh sản.", the move "ĐƯA BƯỚM ĐỰC ĐẾN GẦN BƯỚM CÁI" opens.
  - 7 mating (11389-11429): drag the male close to the female: both stop and face each other, a short animation (no detail of organs) with a connection icon, label GIAO PHỐI, message "Bướm đực và bướm cái giao phối để thực hiện quá trình sinh sản."; too far = "Hãy đưa bướm đực đến gần bướm cái." (male goes to the nearest place). Then THỤ TINH opens.
  - 8-9 fertilization (11431-11494): a diagram screen "TINH TRÙNG CỦA BƯỚM ĐỰC + TRỨNG CỦA BƯỚM CÁI ↓ THỤ TINH"; a sperm icon and an egg icon appear, the sperm moves to the egg, they join with a soft glow, label "TRỨNG ĐÃ THỤ TINH", message "Thụ tinh xảy ra khi tinh trùng của bướm đực kết hợp với trứng của bướm cái."; button "▶ TIẾP TỤC" → back to the garden, the female on the branch, camera to the leaves, message "Sau khi thụ tinh, bướm cái chuẩn bị đẻ trứng trên lá cây."
  - 10-11 egg laying (11496-11556): drag BƯỚM CÁI from the tray to a leaf: lands on the underside, wings tremble, message "Hãy quan sát bướm cái đẻ trứng trên lá cây."; then it lays a small cluster of eggs, flies away, label TRỨNG, message "Bướm cái đẻ trứng trên mặt dưới của lá."; outside a leaf = "Hãy đặt bướm cái lên lá cây." (back to tray).
  - 12-13 egg stage NGÀY 0 → 3 (11558-11622): small white round eggs under the leaf, label "TRỨNG BƯỚM"; drag the magnifier to the eggs: enlarged, message "Trứng bướm rất nhỏ và bám trên mặt dưới lá.", mark "ĐÃ QUAN SÁT TRỨNG ✓", button "▶ CHUYỂN SANG GIAI ĐOẠN NỞ"; wrong = "Hãy đưa kính lúp đến cụm trứng để quan sát."
  - 14 hatching NGÀY 3 → 7 (11626-11666): day 3 eggs dark grey and cracking, message "Trứng đang phát triển và chuẩn bị nở."; day 7 the shell cracks, the larva comes out, eats the shell, crawls to the leaf; bar "SÂU NON ✓".
  - 15-16 larva (11668-11731): big label "SÂU NON (ẤU TRÙNG)", message "Sâu non ăn rất nhiều lá cây, lớn nhanh và lột xác nhiều lần để tăng kích thước."; drag LÁ CÂY TƯƠI near the larva: it turns, crawls, gnaws, the leaf edge gets bites and shrinks, the larva grows a little; message "Sâu non đang ăn lá.", mark "ĐÃ CHO SÂU ĂN ✓"; wrong = "Hãy đưa lá cây đến gần sâu non." (leaf back to tray).
  - 17-18 growth (11733-11768): timeline "LẦN 1 → LẦN 2 → LẦN 3"; each time longer and bigger, keeps eating, then moults (7 steps: stops, old skin splits, crawls out, new bigger body, old skin stays, continues); labels "LẦN LỘT XÁC 1/2/3".
  - 19 ruler (11770-11806): drag THƯỚC ĐO near the larva: it aligns parallel to the body and shows the length; message "Chiều dài sâu non đang tăng lên."; updates after each moult; goal "SÂU NHỎ → SÂU LỚN HƠN → SÂU TRƯỞNG THÀNH"; wrong = "Hãy đưa thước đến gần sâu non để đo." (back to the nearest place).
  - 20 pupation (11808-11832): when big enough it stops eating, leaves the leaf, climbs the branch, hangs in a J shape, last moult, pupal case; label NHỘNG, message "Sâu non đã hóa thành nhộng."; bar "SÂU NON ✓ → NHỘNG".
  - 21-22 pupa NGÀY 14 → 18 (11834-11892): fixed on the branch, message "Bên ngoài nhộng bất động, nhưng bên trong cơ thể đang tiếp tục biến đổi."; drag the magnifier to it: camera zooms, pupa highlighted, button "[ MẶT CẮT 3D ]", mark "ĐÃ QUAN SÁT NHỘNG ✓"; wrong = "Hãy đưa kính lúp đến gần nhộng để quan sát."
  - 23-26 cut (11894-11994): the case splits lengthwise or turns about 70% transparent, camera zooms, label "MẶT CẮT BÊN TRONG NHỘNG"; four clickable forming parts: wings "CÁNH ĐANG HÌNH THÀNH", eyes "MẮT KÉP ĐANG HÌNH THÀNH", legs "CHÂN KHỚP ĐANG HÌNH THÀNH", proboscis "VÒI HÚT ĐANG HÌNH THÀNH"; a click elsewhere = "Hãy nhấp vào bộ phận đang hình thành bên trong nhộng."; after the required parts: "ĐÃ QUAN SÁT BÊN TRONG NHỘNG ✓" and the button "▶ TUA NHANH THỜI GIAN VŨ HÓA".
  - 27-29 emergence NGÀY 18 → 28 (11998-12070): the pupa closes, the case changes, turns clear, a butterfly shape shows inside, the case cracks; message "Bướm đang chuẩn bị thoát khỏi vỏ nhộng."; then 10 steps (crack, crawl out, cling to the branch, wet crumpled wings, cannot fly yet, wings expand, dry, spread, can fly); label "BƯỚM TRƯỞNG THÀNH"; message "Bướm thoát khỏi vỏ nhộng, giương rộng đôi cánh khô ráo và có thể bay đi hút mật, bắt đầu vòng đời mới."; bar "TRỨNG ✓ → SÂU NON ✓ → NHỘNG ✓ → BƯỚM TRƯỞNG THÀNH ✓" and the whole chain done.
  - 30-34 sorting (12074-12148): activity "SẮP XẾP VÒNG ĐỜI CỦA BƯỚM", task "Hãy kéo các thẻ và sắp xếp đúng vòng đời của bướm."; four cards BƯỚM TRƯỞNG THÀNH, NHỘNG, TRỨNG, SÂU NON into VỊ TRÍ 1..4; right order TRỨNG → SÂU NON → NHỘNG → BƯỚM TRƯỞNG THÀNH; right = "Chính xác! Em đã sắp xếp đúng vòng đời của bướm." (cards glow, ✓); wrong = "Chưa đúng. Hãy kiểm tra lại thứ tự các giai đoạn phát triển." (card back to the nearest place or the unsorted area) plus the hint "TRỨNG → SÂU NON → NHỘNG → BƯỚM TRƯỞNG THÀNH"; summary "Bướm phát triển qua 4 giai đoạn: trứng, sâu non, nhộng và bướm trưởng thành. Bướm trưởng thành tiếp tục sinh sản, tạo ra thế hệ bướm mới."
  - 35 final diagram (12152-12194): BƯỚM ĐỰC + BƯỚM CÁI ↓ GIAO PHỐI ↓ THỤ TINH ↓ BƯỚM CÁI ĐẺ TRỨNG ↓ TRỨNG ↓ SÂU NON ↓ NHỘNG ↓ BƯỚM TRƯỞNG THÀNH ↓ TIẾP TỤC SINH SẢN ↓ THẾ HỆ BƯỚM MỚI, animated one after another along the arrows.
  - 36 lock/unlock (12198-12344): 17 rules in the order above (no mating before both butterflies, no fertilization before mating, no egg laying before fertilization, no eggs before the female is on the leaf, no hatching button before the magnifier saw the eggs, larva not fully growing before it was fed, no pupa before the larva is big enough, no cut before the magnifier is on the pupa, no fast forward before the inside was seen, no adult before the fast forward, no sorting before the adult, not complete before the sorting is right).
  - Reset list (11114-11169): stop timeline and every animation; everything back to the tray; remove eggs, larvae, old skins, pupa, pupal case, new butterfly; leaf whole again; cut off; camera back; windows, messages and tooltips cleared; bar back to the first step.
  - Ideas for the build: `stepStates` for the eight bar steps; a leaf with bites (shrinking bites or a texture mask); a larva model with `setLength(l)` and a moult effect; a ruler that follows the larva; a pupa with `setCut(k)` (70% transparent) and four clickable inner parts; an adult butterfly with `setWings(k)` for expanding wings; a sorting activity like the cards of Experiment 6 (`Lab.drag` cards onto slots); days shown with `UI.caption`.
  - REST OF THE SPEC (12362-13163, now read): general message "Hãy đặt bướm vào khu vực quan sát." (12382); a locked button shows "Hãy hoàn thành bước trước để tiếp tục." on hover or click (12708); the time bar is NGÀY 0, 3, 7, 14, 18, 28 (12480-12502; it need not run in real time; days 0 = adults, mating, fertilization, laying; 3 = eggs darker; 7 = hatching; 14 = larva done and pupation; 18 = the pupa changes inside; 28 = the pupa cracks, the adult appears); states of the parts: male TRONG KHAY → ĐANG KÉO → ĐÃ ĐẶT → ĐẬU TRÊN CÀNH → THAM GIA GIAO PHỐI; female TRONG KHAY → ĐÃ ĐẶT → ĐẬU TRÊN CÀNH → SAU THỤ TINH → ĐẬU TRÊN LÁ → ĐẺ TRỨNG → BAY ĐI; egg CHƯA CÓ → TRỨNG MỚI ĐẺ → ĐANG PHÁT TRIỂN → SẮP NỞ → NỨT → ĐÃ NỞ; larva CHƯA XUẤT HIỆN → SÂU NON NHỎ → LỚN LẦN 1, 2, 3 → ĐỦ LỚN → HÓA NHỘNG; pupa CHƯA CÓ → MỚI → PHÁT TRIỂN → ĐƯỢC QUAN SÁT BÊN TRONG → SẮP VŨ HÓA → NỨT → BƯỚM CHUI RA; new butterfly TRONG VỎ → VỪA CHUI RA → CÁNH ẨM → NHĂN → MỞ RỘNG → KHÔ → BƯỚM TRƯỞNG THÀNH. Three feedback levels (12685-12708): right (soft glow, ✓, message, next step opens), wrong (small shake, guiding message, item goes back), not allowed yet (button locked). The 18 state-machine steps and the variable names are in 12747-12858 (`malePlaced … experimentCompleted`, `growthStage`, `moltingCount`, `currentDay`, `magnifierTarget`, `rulerTarget`, `leafTarget`). Reset rule (12884-12900): nothing may stay (eggs, eaten leaf, larva, pupa, adult, cut open, timeline running, lit bar, open buttons, old messages, zoomed camera). Cameras (12420-12474): garden overview while placing; slight zoom on the two butterflies for mating; leaf for laying; underside of the leaf for the eggs; steady with the magnifier; follows the larva on the leaf; larva and ruler when measuring; follows the larva from the leaf up the branch for pupation; on the pupa; deep into the pupa for the cut; fixed on the pupa while emerging; a little wider for the adult; the sorting activity in the centre.
  - FINAL SCREENS: LIII (12904-12924) "HOÀN THÀNH THÍ NGHIỆM" and "Em đã quan sát được quá trình sinh sản và vòng đời của bướm." then the chain with "→ ĐẺ TRỨNG" (not "BƯỚM CÁI ĐẺ TRỨNG"); LIV (12928-12936) the box "KẾT LUẬN" with two sentences: "Bướm là động vật đẻ trứng. Trong quá trình sinh sản, bướm đực và bướm cái giao phối, sau đó xảy ra thụ tinh. Bướm cái đẻ trứng trên lá cây." and "Bướm phát triển qua 4 giai đoạn: trứng → sâu non → nhộng → bướm trưởng thành. Bướm trưởng thành tiếp tục sinh sản, tạo ra thế hệ bướm mới."; LVIII (13126-13160) the last screen shows the title, the chain, the message "Em đã quan sát được sự sinh sản và vòng đời của bướm từ khi bướm trưởng thành sinh sản cho đến khi hình thành bướm trưởng thành mới." and another conclusion "Bướm đẻ trứng và phát triển qua 4 giai đoạn: trứng, sâu non, nhộng và bướm trưởng thành. Bướm trưởng thành tiếp tục sinh sản, tạo thành một vòng đời mới." (the md has two similar conclusions and two chain wordings: note it in `SPEC-NOTES.md`, show both texts on the last screen and keep the chain with "BƯỚM CÁI ĐẺ TRỨNG" for the bar and "ĐẺ TRỨNG" in the final chain, as the md writes them).
- Next: write `js/content/exp7.vi.js` (run `node --test "tests/text-fidelity.test.js"` to check every `md` sentence against the spec), then the models, the wiring and the browser check. Everything is pushed; the Experiment 7 files are committed as work in progress.

### 2026-09-20 (later), Claude: Experiment 6 done (uncommitted; some polish left)

- Built: `js/logic/exp6.js` + `tests/exp6.test.js` (9 tests), `js/content/exp6.vi.js` (every `md` string checked; spec lines 8942-10941 read in full), `js/models/sprouts.js` (`Mo.sproutPot(kind)`: the half-cut pot of Experiment 1 with one sample; `setLife(t)` grows it, t = 1 plantlet at day 14, t = 2 grown plant; `showSample`, `lensPoint`, `zoneBox`, `setSoil`), `js/experiments/exp6.js` (core: four places, 11-card tray, panel, drops, watering, magnifier) and `exp6-lapse.js` (start button, days 0 → 2 → 4 → 7 → 14, click a pot to zoom, classification with four cards, "TIẾP TỤC TUA NHANH", the end). Choices and gaps are in `SPEC-NOTES.md`; `HUONG-DAN.txt` has "Cách chơi thí nghiệm 6". Shared: `index.html` tags, `css/app.css` (place tags, five-day time bar, result chains, and a smaller progress bar when there are eight steps).
- Tested: all 88 tests pass. In a browser (real pointer events, time by hand): pots (wrong place message), samples (wrong pot, outside), water outside a pot, the soil card, the magnifier, the start button, days 0 to 14 with the germination sentence, clicking a pot to zoom and the back button, the classification (a wrong card goes back, four right cards), growing up and the final screen with the progress bar at 8 of 8. The console had no errors. A reset after a full run leaves the same snapshot except textures +2 (the cached "moist" soil textures) and geometries +1 (two full cycles in a row gave the same numbers, so these are cached items and not a leak).
- Not tested: sound, touch screens, Firefox and Safari, file:// captures in headless Edge (the scenarios `pots|samples|lens|water|d4|d14|zoom|classify|finale` are in `tests/shots.js` but were not run), the diagram and conclusion cards (they render but were not looked at), Experiments 1 to 5 after the last changes (they were not touched, but run the smoke check).
- Polish left: (1) the plants look small in the four-pot view (leaves were just made 1.5 times bigger; the stems in `life()` of sprouts.js can be taller); (2) look at the result tags over the pots with the wide panel open; (3) the sweet potato vine and the kalanchoe plantlets are simple shapes: check them with a click-to-zoom and adjust; (4) run the `tests/shots.js` scenarios for Experiment 6 in headless Edge (`?e=exp6`) and look at the pictures.
- Next: Experiment 7 (spec lines 10942-13163). Nothing is pushed. The user later said yes to committing Experiments 2, 4, 5 and 6 (done on 2026-09-20, one commit).

### 2026-09-20 (later), Claude: Experiment 5 done (uncommitted)

- Built: `js/logic/exp5.js` + `tests/exp5.test.js` (10 tests), `js/content/exp5.vi.js` (every `md` string checked against the spec, lines 6728-8941 read in full), `js/models/tomato.js` (the flower, cut lengthwise with clipping planes; the pollen stick; the pollen tube; the fruit that grows, ripens and is cut in half; a seed), `js/experiments/exp5.js` (core: scene, tray, side panel, placing, info table, magnifier) with `exp5-look.js` (the eight parts), `exp5-pollen.js`, `exp5-fert.js` (germination, tube, ovule, male cell) and `exp5-fruit.js` (fast forward, fruit, seeds, the end); each adds itself to `Lab.exp5mods` and shares the object `X`. Choices and gaps are in `SPEC-NOTES.md`; `HUONG-DAN.txt` has "Cách chơi thí nghiệm 5".
- Small changes in shared files: `css/app.css` (glowing dots, loaded-stick badge, time bar), `index.html` (tags), `tests/shots.js` (scenarios `placed|lens|cut|look|ready|pollen|loaded|fert|germ|tube|ovule|male|d7|d14|seeds|d30|finale`). Experiment 5 switches `renderer.localClippingEnabled` on for the cut flower. Note for the next experiments: `Lab.stage.shot` never goes closer than a camera distance of 3, so `w` below about 0.9 has no extra effect.
- Tested: all 78 tests pass. In a browser (real pointer events, time driven by hand) I played the whole story: placing the flower and the md messages for tools too early, the info table, the magnifier and the structure view, all eight dots (another order gives a hint), the stick (no pollen, wrong places, collecting, wrong places with pollen, pollinating), the germination click (a wrong click shows the md sentence), the tube (wrong release, half way, to the end), the ovule (wrong, right), the male cell (wrong, right), the fertilization, the fast forward (days 3, 7, 14 with the seeds, 21, 30), the cut fruit and the final screen. A reset after a full run leaves the same snapshot as a fresh start except one cached glow texture (it does not grow over two cycles). Experiments 1 to 4 still open and take a first drop; the console had no new errors. In headless Edge from `file://` I captured `look` and `germ` (1280x720); `finale` timed out once, like the flaky runs before.
- Not tested: sound, touch screens, Firefox and Safari, keyboard use, small window sizes for the cut halves.
- Next: Experiment 6 (spec lines 8942-10941). Nothing is pushed. (Committed later together with Experiment 6.)

### 2026-09-20 (later), Claude: Experiment 4 done (uncommitted)

- Built: `js/logic/exp4.js` + `tests/exp4.test.js` (10 tests), `js/content/exp4.vi.js` (every `md` string checked against the spec, lines 4859-6727 read in full), `js/models/rat.js` (white rat, see-through body with lungs, an airway from the nose, a stomach and a bladder), `js/experiments/exp4.js` (core: scene, tray, side panel, placing the rat, info table, magnifier, cameras `overview|air|waste|result`) with `exp4-air.js`, `exp4-wfw.js` and `exp4-finale.js` (each adds itself to `Lab.exp4mods` and shares the object `X`). Choices and gaps are in `SPEC-NOTES.md`; `HUONG-DAN.txt` has "Cách chơi thí nghiệm 4".
- Small changes in shared files: `css/app.css` (marks, result diagram, replay buttons, flow label colours), `index.html` (tags), `tests/shots.js` (scenarios `placed|info|air|airGo|food|water|later|waste|wfwGo|finale|replay`), `HUONG-DAN.txt`, `SPEC-NOTES.md`.
- Tested: all 67 tests pass. In a browser (real pointer events, time driven by hand) I played both orders (gases first, then water/food/waste first): wrong drops with the md messages, O₂ along the airway, CO₂ out, the bowl before the food, tools too far from the rat, tools while the rat is busy, waste dragged and tapped, urine dragged, the result screen, the replay with pause, resume and restart, the back button, the magnifier on the rat and on the food tray, the info table. A reset leaves the same snapshot as a fresh start (one extra glow texture, which is a cache, not a leak). Experiments 1 to 3 still open. The console had no errors. In headless Edge from `file://` I captured placed, air, waste, replay (1280x720) and finale (1024x768).
- Not tested: sound, touch screens, Firefox and Safari, smoothness in a visible window, the finished view on a small phone. Two headless runs timed out (`finale` once, `food` once) while the same scenarios worked on other runs, so it is probably the headless browser and not the page.
- Next: Experiment 5 (spec lines 6728-8941, staged locked steps). Nothing is pushed. (Committed later together with Experiment 6.)

### 2026-09-20 (later), Claude: Experiment 2 done (uncommitted)

- Built: `js/logic/exp2.js`, `js/content/exp2.vi.js`, `js/models/cabbage.js` (cabbage in the half-cut pot, plus the clear box), `js/models/molecules.js` (O₂, CO₂, magnifier), `js/experiments/exp2.js` (core) with `exp2-gas.js`, `exp2-water.js`, `exp2-tools.js`, `exp2-finale.js` (each adds itself to `Lab.exp2mods`), `tests/exp2.test.js` (10 tests). Gaps and choices are in `SPEC-NOTES.md`; `HUONG-DAN.txt` has "Cách chơi thí nghiệm 2".
- New shared pieces: `js/core/scenedrag.js` (drag things inside the 3D picture), `js/three/lens.js` (magnifier lens drawn with a second camera), `Lab.fx.flow` (beads along a path), progress bar state `open` (`Lab.ui.setProgress` takes tooltips as a 3rd argument), label anchor `center`, hook `Lab.stage.afterRender`, `Lab.app.snapshot` also counts `sceneItems` and `lens`, `js/models/plants.js` exposes `plantGroup`, `rootMat`, `rootsGroup`.
- Tested: all 56 tests pass. In a browser I played gas exchange first, then water and minerals, using real pointer drags (wrong and right moves, the md messages), the finale, "TƯƠNG TÁC TỔNG HỢP", the magnifier, the box and the back button. A reset at the end leaves the same snapshot as a fresh start. Experiments 1 and 3 still play to their results. The console had no errors.
- Also tested: water and minerals before gas exchange (minerals before water, photosynthesis before respiration), and real `file://` captures in headless Edge (1280x720: respiration, roots, everything at once; 1024x768: the finale). Scenarios `placed|resp|photo|root|lens|finale|synth` are in `tests/shots.js`.
- Not tested: sound, touch screens, Firefox and Safari, smoothness in a visible window (time was driven by hand), the box's look up close.
- Next: Experiment 4 (lab rat, spec lines 4859-6727). Nothing is pushed. (Committed later together with Experiment 6.)

