---
description: Generate a Conventional Commit title (Vietnamese) and a frontend PR description from the current changes
argument-hint: "[base-branch | --staged | <commit-range>]"
---

Invoke the `frontend-pr-generator` skill and follow it exactly.

Target for the diff (may be empty, then use the skill's default): $ARGUMENTS
